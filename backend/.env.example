# ===== DATABASE =====
DATABASE_URL="postgresql://xch_user:XchSecure2024!@postgres:5432/xch_dev"

# ===== APP =====
NODE_ENV=development
PORT=3000

# ===== FRONTEND =====
# Only needed if frontend is on a different origin (CORS)
# When behind nginx proxy, leave empty
FRONTEND_URL=

# ===== COOKIES =====
# Domain for cookies (leave empty for localhost, set to .yourdomain.com for production)
COOKIE_DOMAIN=
# Set to true when using HTTPS in production
COOKIE_SECURE=false

# ===== JWT =====
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-32-chars"
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ===== OIDC (optional) =====
# OIDC_ENABLED=false
# OIDC_ISSUER=https://auth.example.com
# OIDC_CLIENT_ID=xch-app
# OIDC_CLIENT_SECRET=your-oidc-secret
# OIDC_CALLBACK_URL=https://yourdomain.com/api/auth/oidc/callback

# ===== SMTP (Email - optional) =====
# Leave SMTP_HOST empty to log emails to console (dev mode)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# SMTP_FROM=noreply@xch.local

# ===== REDIS =====
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# ===== MINIO (S3-compatible storage) =====
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=xch_minio_admin
MINIO_SECRET_KEY=XchMinIO2024SecureKey!
MINIO_BUCKET=xch-storage
MINIO_USE_SSL=false
# Public URL for browser access to stored files
# When behind nginx: leave empty (uses /storage/ relative path via nginx proxy)
# When direct access needed: http://localhost:9000 (dev) or https://cdn.yourdomain.com
MINIO_PUBLIC_URL=

# ===== STORAGE =====
# Options: minio (recommended), filesystem
STORAGE_TYPE=minio
UPLOAD_DIR=./uploads

# ===== NETBOX INTEGRATION (optional) =====
# NETBOX_URL=https://netbox.example.com
# NETBOX_TOKEN=your_netbox_api_token_here

# ===== MONITORING INTEGRATION (optional) =====
# Supports: uptime_kuma, gatus
# Can also be configured via Settings > Integrations in the UI

# Uptime Kuma (Prometheus /metrics endpoint)
# UPTIME_KUMA_URL=https://uptime.example.com
# UPTIME_KUMA_USERNAME=
# UPTIME_KUMA_PASSWORD=your_api_key_here

# Gatus (REST API /api/v1/endpoints/statuses)
# GATUS_URL=https://gatus.example.com
# GATUS_API_KEY=your_bearer_token_here

# Webhook secret (shared between monitoring tool and XCH)
# MONITORING_WEBHOOK_SECRET=your-shared-secret-here
